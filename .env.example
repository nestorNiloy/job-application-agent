# Copy this file to .env for local development, or set these as
# environment variables in your Render dashboard. Never commit .env
# with real values — it's already excluded via .gitignore.

ADZUNA_APP_ID=your_adzuna_app_id
ADZUNA_APP_KEY=your_adzuna_app_key
ADZUNA_COUNTRY=de
